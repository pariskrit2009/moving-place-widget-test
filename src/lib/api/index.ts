// Export generic HTTP functions
export { get, post, put, del } from "./client";

// Export React Query hooks from centralized client (legacy)

// Note: Feature-specific hooks should be imported from their respective feature modules
// e.g., import { useQuotes } from "@/features/quote/hooks";
// import { useCheckoutForm } from "@/features/checkout/hooks";
